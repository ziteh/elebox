---
title: Elebox – Lightweight Electronic Parts Inventory Tool
---

import * as React from "react";
import Button from "@mui/material/Button";
import Grid from "@mui/material/Grid";
import Typography from "@mui/material/Typography";
import Box from "@mui/material/Box";
import Stack from "@mui/material/Stack";
import Container from "@mui/material/Container";
import Image from "next/image";

<Container
  maxWidth="xl"
  sx={{
    display: "flex",
    flexDirection: "column",
    paddingTop: 6,
    paddingBottom: 6,
  }}
>
  <Stack spacing={4}>
    <Box sx={{ maxWidth: "md" }}>
      <Typography variant="h3" sx={{ lineHeight: "1.2", fontWeight: 700 }}>
        Lightweight personal electronic parts inventory management software
      </Typography>
    </Box>
    <Box>
      <Typography variant="h6" sx={{ lineHeight: "1.2", fontWeight: 300 }}>
        Simple, intuitive, out-of-the-box, and no need to set up database
      </Typography>
    </Box>
    <Box>
      <Stack direction="row" spacing={2}>
        <Button
          href="https://github.com/ziteh/elebox/releases/latest"
          variant="contained"
          disableElevation
          component="a"
          target="_blank"
          rel="noopener noreferrer"
          sx={{ textTransform: "none" }}
        >
          Download
        </Button>
        <Button
          href="/docs/overview"
          variant="text"
          disableElevation
          sx={{ textTransform: "none" }}
        >
          Overview
        </Button>
      </Stack>
    </Box>
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
      }}
    >
      <Box
        sx={{
          width: 800,
          borderRadius: "8px",
          overflow: "hidden",
        }}
      >
        <Image src="https://i.imgur.com/i6ihU9o.png" width={800} height={300} />
      </Box>
    </Box>
  </Stack>
</Container>
